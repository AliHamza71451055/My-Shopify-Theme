<header class="site-header" x-data="{ open: false }">
    <div class="header-container">
        <!-- Left Section: Shop Name -->
        <div class="shop-name">
            <a href="{{ shop.url }}">{{ shop.name }}</a>
        </div>

        <!-- Middle Section: Navigation Menu -->
        <nav class="desktop-menu">
            <ul>
                {% for link in linklists.main-menu.links %}
                    <li x-data="{ openDropdown: false }" class="menu-item">
                        <a href="{{ link.url }}" @click.prevent="openDropdown = !openDropdown">{{ link.title }}</a>
                        {% if link.links %}
                            <ul class="dropdown" x-show="openDropdown" @click.away="openDropdown = false" x-transition>
                                {% for sublink in link.links %}
                                    <li><a href="{{ sublink.url }}">{{ sublink.title }}</a></li>
                                {% endfor %}
                            </ul>
                        {% endif %}
                    </li>
                {% endfor %}
            </ul>
        </nav>

        <!-- Mobile Menu Toggle -->
        <div class="mobile-menu-toggle">
            <button @click="open = !open" class="menu-button">Menu</button>
        </div>

        <!-- Mobile Menu -->
        <nav class="mobile-menu" x-show="open" @click.away="open = false" x-transition>
            <div class="shop-name-mobile">
                <a href="{{ shop.url }}">{{ shop.name }}</a>
            </div>
            <ul>
                {% for link in linklists.main-menu.links %}
                    <li x-data="{ openDropdown: false }" class="menu-item">
                        <a href="{{ link.url }}" @click.prevent="openDropdown = !openDropdown">{{ link.title }}</a>
                        {% if link.links %}
                            <ul class="dropdown" x-show="openDropdown" @click.away="openDropdown = false" x-transition>
                                {% for sublink in link.links %}
                                    <li><a href="{{ sublink.url }}">{{ sublink.title }}</a></li>
                                {% endfor %}
                            </ul>
                        {% endif %}
                    </li>
                {% endfor %}
            </ul>
            <div class="user-links-mobile">
                <a href="/account">Account</a>
                <a href="/cart">Cart</a>
            </div>
        </nav>
    </div>
</header>

<style>
    .site-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 10px 20px;
        background-color: #ffffff;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    .header-container {
        display: flex;
        width: 100%;
        align-items: center;
        position: relative;
    }

    .shop-name a {
        font-size: 24px;
        font-weight: bold;
        text-decoration: none;
        color: #333;
    }

    .desktop-menu {
        flex-grow: 1;
        text-align: center;
    }

    .desktop-menu ul {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        justify-content: center;
    }

    .desktop-menu .menu-item {
        position: relative;
        margin: 0 15px;
    }

    .desktop-menu a {
        text-decoration: none;
        color: #333;
        padding: 10px;
        transition: color 0.3s;
    }

    .desktop-menu a:hover {
        color: #007bff;
    }

    .dropdown {
        display: none;
        position: absolute;
        top: 100%;
        left: 0;
        background-color: #fff;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        z-index: 1000;
    }

    .dropdown[x-show="true"] {
        display: block;
    }

    .user-links-mobile {
        padding: 10px;
        text-align: center;
    }

    .user-links-mobile a {
        margin: 0 10px;
        text-decoration: none;
        color: #333;
        transition: color 0.3s;
    }

    .user-links-mobile a:hover {
        color: #007bff;
    }

    .mobile-menu-toggle {
        display: none; /* Hide mobile menu toggle on desktop */
    }

    .mobile ```liquid
    .menu {
        display: none; /* Initially hide mobile menu */
    }

    .mobile-menu[x-show="true"] {
        display: block; /* Show mobile menu when toggled */
    }

    @media (max-width: 768px) {
        .desktop-menu {
            display: none; /* Hide desktop menu on mobile */
        }

        .mobile-menu-toggle {
            display: block; /* Show mobile menu toggle on mobile */
        }
    }
</style>

<script>
    document.addEventListener("DOMContentLoaded", function() {
        var mobileMenuToggle = document.querySelector('.mobile-menu-toggle button');
        var mobileMenu = document.querySelector('.mobile-menu');

        mobileMenuToggle.addEventListener("click", function() {
            if (mobileMenu.style.display === "block") {
                mobileMenu.style.display = "none";
            } else {
                mobileMenu.style.display = "block";
            }
        });

        // Optional: Close the mobile menu when clicking outside of it
        document.addEventListener("click", function(event) {
            if (!mobileMenu.contains(event.target) && !mobileMenuToggle.contains(event.target)) {
                mobileMenu.style.display = "none";
            }
        });
    });
</script>